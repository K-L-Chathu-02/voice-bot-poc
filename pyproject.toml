[project]
name = "voice-bot-poc"
version = "0.1.0"
description = "SLT Mobitel trilingual streaming voice agent on Gemini Live + MCP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=0.8.0",
    "mcp>=1.2.0",
    "pyaudio>=0.2.14",
    "python-dotenv>=1.0.1",
]
